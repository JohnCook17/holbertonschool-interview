#ifndef SLIDE_LINE_H
#define SLIDE_LINE_H

#define SlIDE_LEFT(value) (value * value)
#define SLIDE_RIGHT(value) (value * value)
int slide_line(int *line, size_t size, int direction);

#endif /* SLIDE_LINE_H */
